<?xml version="1.0" encoding="UTF-8"?>
<project default="install" name="targetFile">
	<dirname property="targets.dir" file="${ant.file.targetFile}"/>
	<property name="target.props" location="${targets.dir}/targets.properties"/>
	<property file="${target.props}"/>
	
	<echo> OURL:  ${openspace.url}</echo>
	<!-- <property name="target.props" value="targetFile.ant.file"/> -->
	
	<target name="install" >
		<echo message="This is install target"/>
	</target>
	
	<target name="reinstall">
	
		<echo message="This is reinstall target"/>
		<echo message="call targets.prpereties"/>
		<echo> ${openspace.url}</echo>
	</target>
</project>